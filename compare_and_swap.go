package distsemaphore

func compareAndSwap(s *semaphore, old, new uint64) (swapped bool) {



	//res, err := v3.NewKV().Txn(context.Background()).If(
	//	v3.Compare(v3.ModRevision(c.path), "=", c.modRevision),
	//).Then(
	//	v3.OpPut(c.path, string(data)),
	//	v3.OpGet(c.path),
	//).Commit()


	return true
}

// https://stackoverflow.